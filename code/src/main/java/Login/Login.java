package Login;

public class Login {

    public boolean realizarLogin(String nome, String senha) {
        // Simulação básica: Se o nome for "usuarioCorreto" e a senha for "senhaCorreta", o login é válido
        return nome.equals("usuarioCorreto") && senha.equals("senhaCorreta");
    }
}
